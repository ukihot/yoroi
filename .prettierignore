# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/drizzle/

# apps/control is a separate Deno app formatted/linted via `deno fmt`/`deno
# lint` (its own deno.jsonc), not this project's Prettier/ESLint config.
/apps/
